{
  "name": "@OrangeLanding/pc",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "node server-dev.js",
    "serve": "node server-prod.js",
    "build": "vite build --ssrManifest --outDir dist/client && vite build --ssr src/entry-server --outDir dist/server",
    "build:client": "vite build --ssrManifest --outDir dist/client",
    "build:server": "vite build --ssr src/entry-server --outDir dist/server"
  }
}