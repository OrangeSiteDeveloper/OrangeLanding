"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createDev = void 0;
const vite_1 = require("vite");
const plugin_1 = require("../plugin");
const createDev = (options) => async (app) => {
    var _a;
    const viteConfig = vite_1.mergeConfig({
        configFile: false,
        plugins: [
            plugin_1.createPlugin({
                app,
                options,
                isServer: false,
                isBuild: false,
            }),
        ],
        // `clearScreen` won't take effect in `config` hook of plugin API
        clearScreen: false,
    }, (_a = options.viteOptions) !== null && _a !== void 0 ? _a : {});
    const server = await vite_1.createServer(viteConfig);
    await server.listen();
    return server.close.bind(server);
};
exports.createDev = createDev;
