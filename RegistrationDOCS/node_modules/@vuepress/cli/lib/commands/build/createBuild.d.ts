import type { AppConfig } from '@vuepress/core';
import type { BuildCommandOptions } from './types';
export declare type BuildCommand = (sourceDir?: string, commandOptions?: BuildCommandOptions) => Promise<void>;
export declare const createBuild: (defaultAppConfig: Partial<AppConfig>) => BuildCommand;
