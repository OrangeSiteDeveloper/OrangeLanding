"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolvePageFilePath = void 0;
const utils_1 = require("@vuepress/utils");
/**
 * Resolve absolute and relative path of page file
 */
const resolvePageFilePath = ({ app, options: { filePath }, }) => {
    // empty file path
    if (!filePath) {
        return {
            filePath: null,
            filePathRelative: null,
        };
    }
    // absolute file path
    if (utils_1.path.isAbsolute(filePath)) {
        return {
            filePath,
            filePathRelative: utils_1.path.relative(app.dir.source(), filePath),
        };
    }
    // relative file path
    return {
        filePath: app.dir.source(filePath),
        filePathRelative: filePath,
    };
};
exports.resolvePageFilePath = resolvePageFilePath;
