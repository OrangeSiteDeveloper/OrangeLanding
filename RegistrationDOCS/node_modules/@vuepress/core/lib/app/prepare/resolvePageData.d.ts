import type { Page, PageData } from '../../types';
/**
 * Resolve page data for client usage
 */
export declare const resolvePageData: ({ key, path, title, lang, frontmatter, excerpt, headers, }: Page) => PageData;
