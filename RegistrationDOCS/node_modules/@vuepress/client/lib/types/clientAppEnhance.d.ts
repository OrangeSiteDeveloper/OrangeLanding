import type { App } from 'vue';
import type { Router } from 'vue-router';
import type { SiteDataRef } from '../injections';
export declare type ClientAppEnhance = (context: {
    app: App;
    router: Router;
    siteData: SiteDataRef;
}) => void | Promise<void>;
